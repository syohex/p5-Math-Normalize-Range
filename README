This is Perl module Math::Normalize::Range.

INSTALLATION

Math::Normalize::Range installation is straightforward.
If your CPAN shell is set up, you should just be able to do

    % cpan Math::Normalize::Range

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Math::Normalize::Range documentation is available as in POD.
So you can do:

    % perldoc Math::Normalize::Range

to read the documentation online with your favorite pager.

Syohei YOSHIDA
